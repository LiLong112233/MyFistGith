<?php 

   class Hum{
     
     private $sex;
     protected $age;
      
      //当对不存在的属性值赋值的时候调用
      public function __set($name,$value){
      	echo "$name是".$value;
      }

     //当获取一个不存在的属性值或权限不够的属性值时调用
      public function __get($name){
         echo "你好-".$name;
      }
      
      //调用一个不存在的方法或者权限不够的方法时调用
      public function __call($name,$arr){
          echo "不存在的方法";
          echo $name;
          print_r($arr);
      }

      protected function eat(){

      }
  
      //当销毁一个不存在或者权限不够的属性时会被调用
      public function __unset($name){
      	echo "销毁我吧".$name;
      }
     
     //当用isset函数去判断一个不存在或者权限不够的属性时调用
     public function __isset($name){
     	echo "判断你在不".$name;
     }

    //当用echo输出一个对象时被调用
     public function __toString(){
     	echo "你正在使用echo输出一个对象";
     }
   }


   $person = new Hum();

   // $person->run('跑起来',18);

   // $person->eat(12);


   // unset($person->name);
   // unset($person->sex);

   // $person->aaa ="顾生壮";

   // $person->sex="男";
   // $person->age=18;

   // echo $person->sex;

   // echo $person->age;

   // isset($person->sex);//判断权限不够的属性值
   // isset($person->husband);  //wife  husband

   echo $person;
   // var_dump($person);

 ?>
























































































































































































































































































































































































































































































 .






























































































 .......

















































































































































































 .....................




















































































 ................

































































































 ....................................................................................................................